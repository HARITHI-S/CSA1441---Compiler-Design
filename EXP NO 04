%{
#include <stdio.h>
%}

DIGIT      [0-9]
INT        {DIGIT}+
FLOAT      {DIGIT}+"."{DIGIT}+
STRING     \"([^\"\n]*)\"

%%

{STRING}   { printf("String Constant: %s\n", yytext); }  /* handle strings first */
{FLOAT}    { printf("Float Constant: %s\n", yytext); }
{INT}      { printf("Integer Constant: %s\n", yytext); }

[ \t\n]+   ;   /* ignore whitespace */
.          ;   /* ignore other characters */

%%

int main()
{
    yyin = fopen("sample.c", "r");
    if (!yyin)
    {
        printf("Cannot open file\n");
        return 1;
    }
    yylex();
    fclose(yyin);
    return 0;
}

int yywrap()
{
    return 1;
}
