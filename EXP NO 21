%{
#include <stdio.h>
%}

TAG    \<[^>]+\>

%%

{TAG}     { printf("HTML Tag: %s\n", yytext); }

[ \t\n]+  ;   /* ignore whitespace */
.         ;   /* ignore other text */

%%

int main()
{
    yyin = fopen("sample.html", "r");
    if (!yyin)
    {
        printf("Cannot open file\n");
        return 1;
    }

    yylex();
    fclose(yyin);
    return 0;
}

int yywrap()
{
    return 1;
}
